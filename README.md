# Entity Veterinary Hospital

Interactive demo concept and web platform presentation for Entity Veterinary Hospital in Chattogram, Bangladesh, built by Tectonic.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

The app will be available at **http://localhost:3000**.
